{
  "name": "meteora-bot-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "start": "node start.js",
    "build": "yarn workspaces run build"
  }
}
